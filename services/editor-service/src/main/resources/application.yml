spring:
  application:
    name: editor-service
identity:
  api:
    url: ${IDENTITY_API_URL:http://localhost/api/identity}
audit:
  api:
    url: ${AUDIT_API_URL:http://localhost/api/audit}
management:
  endpoints:
    web:
      exposure:
        include: health,info
logging:
  level:
    root: INFO
