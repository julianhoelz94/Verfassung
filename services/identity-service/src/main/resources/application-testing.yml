identity:
  seed:
    mode: create-only
    editor-email: ${TEST_EDITOR_EMAIL:}
    editor-password: ${TEST_EDITOR_PASSWORD:}
    reviewer-email: ${TEST_REVIEWER_EMAIL:}
    reviewer-password: ${TEST_REVIEWER_PASSWORD:}
    publisher-email: ${TEST_PUBLISHER_EMAIL:}
    publisher-password: ${TEST_PUBLISHER_PASSWORD:}
    admin-email: ${TEST_ADMIN_EMAIL:}
    admin-password: ${TEST_ADMIN_PASSWORD:}
    viewer-email: ${TEST_VIEWER_EMAIL:}
    viewer-password: ${TEST_VIEWER_PASSWORD:}
    totp-secret: ${IDENTITY_SEED_TOTP_SECRET:CAATLASMFASEED22}
  session:
    idle-timeout: 15m
    absolute-timeout: 8h
  password:
    log-reset-token: true
