rootProject.name = "constitution-atlas-wrapper"

// Independent services live in services/<name>/. This file only exists so the
// repo-root Gradle wrapper can be generated; it is not a multi-module build.
// Run tasks with: ./gradlew -p services/<name> <task>
// or: cd services/<name> && ./gradlew <task>
